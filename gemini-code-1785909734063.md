# 🛠️ Word Typing Defense 모듈화 및 기능 개편 작업 체크리스트

기존 단일 `game.js` 중심 구조에서 모듈화 분리 및 2단 몬스터 UI, 다중 채널 연동, OBS 오버레이 기능을 적용하기 위한 파일별 수정·생성 작업 가이드입니다.

---

## 1. 🔄 기존 파일 수정 목록 (Modified Files)

### 📄 `js/game.js` (전면 재작성)
- [ ] 기존 비대한 로직(몬스터 스폰, 렌더링, 입력 처리 등) 전면 제거
- [ ] 분리된 서브 모듈들(`StateManager`, `TurretManager`, `MonsterManager`, `InputManager`, `CanvasRenderer`) 인스턴스화 및 연결
- [ ] 메인 루프(`requestAnimationFrame`)를 돌리며 업데이트 및 렌더링을 하달하는 **중앙 오케스트레이터(Orchestrator)** 역할 구현

### 📄 `js/wordPacks.js`
- [ ] `getNextMonsterData()` 함수 추가: 시청자 닉네임(부족 시 `[BOT]`)과 clean 제시어를 분리하여 반환하는 2단 구조 대응
- [ ] `getHangulStrokeCount()` 유틸리티 함수 추가: 한글 초성/중성/종성 자모 획수 정밀 분석 로직

### 📄 `js/chatIntegration.js`
- [ ] 단일 채널 연동 방식에서 배열 기반 다중 채널 동시 연동 구조(`channels[]`)로 확장
- [ ] 시청자 부족 시 `[BOT]` 가상 시청자 자동 참가 처리

### 📄 `js/config.js`
- [ ] 카카오 애드핏 728x90 배너 ID 관리 배열/객체 추가
- [ ] DOM 로드 완료 후 `initializeAds()` 자동 실행 연동

### 📄 `index.html`
- [ ] `js/core/` 및 `js/renderers/` 서브 모듈 스크립트 태그를 올바른 의존성 순서로 추가
- [ ] 메인 화면 1~6인 선택 카운터 및 옵션 UI 구축
- [ ] 카카오 애드핏 배너 슬롯 7개(`class="ad-banner-box"`) 작성
- [ ] `#modal-support` 내부 카카오페이 송금 QR 이미지 모달 구축

### 📄 `style.css`
- [ ] 모바일 미디어 쿼리 전면 제거 및 `1024x768` PC 최소 해상도 고정 스타일 적용
- [ ] 모달 세로잘림 방지 (`max-height: 80vh`, 내부 스크롤) 적용
- [ ] `.ad-banner-box` 최소 높이(106px) 확보로 Layout Shift 방지
- [ ] OBS 방송용 배경 투명 오버레이(`body.obs-overlay`) 및 크로마키 배경 스타일 추가

---

## 2. 🆕 신규 모듈 파일 생성 목록 (New Files)

`js/core/` 및 `js/renderers/` 디렉터리를 신설하고 아래 5개 모듈을 작성합니다.

### 📂 `js/core/`

#### 📄 `js/core/StateManager.js` (상태 관리자)
- [ ] State Machine (MENU, SETTING, READY, PLAYING, PAUSE, GAME OVER, RESULT) 전환 및 이벤트 관리
- [ ] 점수, HP, 스테이지 카운트 상태값 보관
- [ ] `calculateRankGrade()` (SSS~D 등급 환산) 및 `saveLeaderboardRecord()` (localStorage 전적 저장) 로직 구현

#### 📄 `js/core/TurretManager.js` (포탑 엔진)
- [ ] 참여 인원(1~6명)에 따른 Canvas 하단 N개 포탑 균등 배치 좌표 연산
- [ ] **통합 입력모드**: 타깃 몬스터와 가장 가까운 포탑 자동 선정 및 회전 각도($\theta$) 계산
- [ ] **개별 입력모드**: P1~PN 지정 포탑별 발사 제어

#### 📄 `js/core/MonsterManager.js` (몬스터 물리 엔진)
- [ ] Max Monster Cap (동시 최대 15마리) 상한선 제한 및 스폰 대기열 관리
- [ ] 5 Stage 단위 보스전(WARNING) 및 일반 몬스터 Y축 이동 좌표 업데이트
- [ ] 바닥 도달 시 HP 차감 및 화면 제거 판정

#### 📄 `js/core/InputManager.js` (입력 처리 엔진)
- [ ] 단일/개별 Input 태그 이벤트 수신 및 한글 조합 완료(IME) 감지
- [ ] `wordPacks.js` 유틸리티와 연동하여 한글 자모 획수 기반 정밀 CPM/WPM 실시간 계산

### 📂 `js/renderers/`

#### 📄 `js/renderers/CanvasRenderer.js` (그래픽 렌더러)
- [ ] `devicePixelRatio` 스케일링 기반 Retina/4K 대응 Canvas 고해상도 Draw
- [ ] **2단 몬스터 UI Draw**: 상단 Pill Tag(시청자 닉네임/BOT) + 하단 Target Box(제시어) 분리 렌더링
- [ ] 레이저 빔, 폭발 파티클, FEVER 이펙트 및 Boss WARNING 연출 전담

---

## 3. 📌 의존성 로드 순서 (`index.html`)

Script 로드 시 아래 의존성 순서를 준수해야 오류가 발생하지 않습니다.

```html
<script src="js/config.js"></script>
<script src="js/wordPacks.js"></script>
<script src="js/audio.js"></script>

<script src="js/core/StateManager.js"></script>
<script src="js/core/TurretManager.js"></script>
<script src="js/core/MonsterManager.js"></script>
<script src="js/core/InputManager.js"></script>

<script src="js/renderers/CanvasRenderer.js"></script>

<script src="js/chatIntegration.js"></script>
<script src="js/game.js"></script>