# 🎮 스트리머 워드 디펜스 (1인 솔로 싱글 플레이 최적화) 구현 계획서

스트리머 1인 솔로 플레이에 최적화된 단일 입력 디펜스 환경을 제공하며, 화면상에는 시청자 닉네임 뱃지와 타깃 제시어(밈/유행어)가 직관적으로 구분되는 2단 몬스터 UI 시스템을 구축합니다.  
모바일 접속은 완전히 배제하고 최소 지원 해상도를 **1024x768(PC 전용)**로 확정하여, 이 규격의 모니터 환경에서도 화면 잘림이나 가로 스크롤 없이 100% 쾌적하고 안정적인 방송 플레이를 보장합니다.

---

## 🎯 주요 개선 목표 및 정책

1. **🏷️ 2단 몬스터 UI (상단: 시청자 닉네임 뱃지 / 하단: 밈·유행어 제시어)**
   - **상단 Pill Tag**: 실제 시청자 닉네임 표시.
     - 시청자가 부족하여 시스템이 자동 소환하는 기본 블록 몬스터는 `[BOT] 자동소환봇` 등의 표식을 붙여 개발자/시스템 데이터임을 명확히 분리.
   - **하단 Target Box**: `[BOT]` 블록일 때도 하단 텍스트는 기존 밈이나 단어 팩에서 랜덤으로 선택된 게임용 단어를 표시하여 타자 디펜스 재미 유지.

2. **🛡️ 대형 방송 마비 방지 시스템 (Max Monster Cap = 15)**
   - 화면에 동시에 출전하는 몬스터 개수를 최대 15개로 제한하여 대형 스트리머 방송에서도 브라우저 렉 및 화면 마비 방지.

3. **💻 최소 데스크탑 모니터 기준 UI 및 고정 바운드 최적화 (1024x768 타겟 고정)**
   - **기준 해상도 확정**:
     - 애매한 반응형 및 모바일 기준을 제외하고 `1024x768` 해상도를 절대적인 최소 동작 기준(PC-Only)으로 확정.
   - **Desktop Fluid Typography**:
     - `clamp()` 타이포그래피를 적용하여 4K 고해상도부터 최소 지원 해상도인 `1024x768`까지 최적의 글자 가독성 보장.
   - **Vertical Modal Control (세로 잘림 방지)**:
     - 세로 해상도 768px 환경에서도 화면이 잘리지 않도록 모달 창 높이를 `max-height: 80vh`로 제한하고 내부 독립 스크롤 적용.
   - **Horizontal Overflow 차단 (가로 스크롤 방지)**:
     - 728x90 광고 탑재를 위해 카드 너비를 800px까지 확장하더라도 최소 1024px 화면 안에서 좌우 여백을 확보하도록 고정형 바운드 적용.
     - `width: min(96%, 800px)`

4. **👥 자유로운 인원 수 선택 (1명 ~ 6명)**
   - **1인 솔로 선택 시**
     - 대결/협동 및 개별/통합 입력창 등 복잡한 합방 전용 옵션을 자동 숨김 처리.
     - `1인 솔로 방어` 모드로 고정.
   - **2인 이상 합방 선택 시**
     - 대결 모드(Versus Battle) 또는 협동 모드(Co-op Raid) 선택 기능 제공.
     - 입력창 방식 선택 옵션 동적 노출.
   - 각 플레이어별 닉네임 및 포탑 전용 네온 색상 개별 지정.

5. **💥 동적 다중 포탑 및 최적 조준 엔진 (Multi-Turret Engine)**
   - 선택된 인원 수에 따라 Canvas 하단에 포탑 N개 자동 균등 배치.
   - 각 포탑별 닉네임 뱃지 및 개별 레이저/파티클 렌더링.
   - 통합 입력창 사용 시 낙하 몬스터 위치와 가장 가까운 포탑이 자동 조준 사격.

6. **⌨️ 다중 입력 방식 지원 (Multi-Input System)**
   - **개별 입력모드**
     - 화면 하단에 P1 ~ PN 플레이어별 개별 입력창 생성.
     - 각 플레이어가 자신의 전용 포탑으로 공격.
   - **통합 입력모드**
     - 단일 입력창 1개 제공.
     - 주 스트리머 또는 관전자/MC가 전체 몬스터 타격 가능.

7. **🎮 게임 상태 관리 (State Machine)**
   - 게임 상태:
      - 1. MENU: 메인 타이틀 및 모드 선택 대기 상태
      - 2. SETTING: 참여 인원, 난이도 및 플레이어 닉네임 설정 상태
      - 3. READY: 게임 시작 전 카운트다운 및 준비 연출 상태
      - 4. PLAYING: 실시간 몬스터 낙하 및 타자 사격 진행 상태
      - 5. PAUSE: 일시 정지 상태
      - 6. GAME OVER: 체력 고갈에 따른 승패 판정 연출 상태
      - 7. RESULT: 최종 성과 등급(SSS~D) 및 리더보드 출력 상태
   - 현재 게임 상태에 따라 UI와 입력 이벤트를 자동 제어하여 중복 실행 및 예외 상황 방지.
   - 상태 전환은 게임 엔진에서 중앙 관리하며 모든 시스템은 현재 State 기준으로 동작.

8. **📡 실시간 방송 채팅 연동 (`!참여` 자동 참가)**
   - 스트리머는 GitHub Pages에 배포된 웹 게임 URL에 접속하여 게임 실행.
   - 스트리머 방송 URL 또는 채널 ID 연동 후 시청자가 `!참여`, `!참가` 명령어 입력 시 자동 참가.
   - 시청자는 별도의 웹사이트 접속이나 프로그램 설치 없이 방송 채팅만으로 게임 참여 가능.

9. **🎯 한글 자모 획수 기반 CPM/WPM 계산 및 HiDPI 고해상도 최적화**
   - 한글 초성/중성/종성 획수를 감안한 정밀 타수(CPM) 계산.
   - CPM과 WPM 동시 제공.
   - `devicePixelRatio` 스케일링으로 4K/Retina 환경에서도 선명한 Canvas 렌더링 지원.
   - 키보드 입력 포커스 안정적 유지.

10. **🎯 난이도 4단계 선택 및 ♾️ 무한 웨이브 스테이지 (Endless Stage) 시스템**
   - **난이도 4종**
     - Easy
     - Normal
     - Hard
     - Hell
   - **무한 Stage 진행**
     - 게임 종료 엔딩 없이 플레이어가 패배할 때까지 진행.
     - Stage 증가에 따라 몬스터 난이도 점진 상승.
     - 몬스터 처치 시 `STAGE 1 → STAGE 2` 연출 배너 및 상승 효과음 재생.
   - **Boss Wave**
     - 5 Stage 단위마다 대형 보스 몬스터 등장.
     - WARNING 연출 및 보스전 효과 적용.

11. **👑 성과 등급 뱃지(SSS~D) 및 🏆 로컬 명예의 전당 TOP 5 리더보드**
   - **Rank Grade**
     - 점수, Stage, 난이도 종합 계산.
     - `SSS`, `SS`, `S`, `A`, `B`, `C`, `D` 등급 제공.
   - **Local Leaderboard**
     - 브라우저 `localStorage` 기반 전적 저장.
     - TOP 5 갱신 시 `NEW RECORD!` 축하 이펙트 출력.

12. **💰 웹 게임 수익화 (카카오 애드핏 728x90 PC 전용 배너 & ☕ 카카오페이 QR 후원 모달)**
   - **Kakao AdFit**
     - PC 환경 최적화를 위해 `728x90` 가로형 띠배너 규격 사용.
   - **후원 모달**
     - 상단바 `[☕ 후원]` 버튼 클릭 시 카카오페이 송금 QR 모달 표시.
     - 계좌번호 등 개인정보 노출 없이 QR 코드 스캔 방식 지원.
   - **광고 배너 위치**
     1. 메인 화면 최하단
     2. 게임 오버 결과창 카드 하단
     3. 명예의 전당 리더보드 모달 하단
     4. 단어/닉네임 팩 설정 모달 하단
     5. 방송 채팅 연동 설정 모달 하단
     6. 카카오페이 후원 모달 하단
   - 모든 광고 ID는 `js/config.js`에서 통합 관리.

13. **🌐 GitHub Pages 무료 웹 배포**
   - Repository:
     ```
     https://github.com/golddragon0207/word-typing-defense.git
     ```
   - Live Web URL:
     ```
     https://golddragon0207.github.io/word-typing-defense/
     ```
---

## 📂 프로젝트 파일 구조 및 역할

1. **`index.html`**
   - 메인 화면에 참여 인원 조절 카운터 (1P ~ 6P), 난이도 선택(Easy, Normal, Hard, Hell) 및 플레이어 닉네임 설정 UI 구축.
   - 상단 컨트롤바에 `🏆 명예의 전당 (Top 5)` 및 `☕ 후원` 모달 버튼 배치.
   - 총 7개의 728x90 카카오 애드핏 배너 슬롯 (`class="ad-banner-box"`) 배치.
   - 결과 화면에 `👑 SSS~D RANK 뱃지`, `🎉 NEW RECORD!` 태그 및 최종 도달 스테이지 출력 구조 작성.
   - `#modal-support` 내부에 카카오페이 송금용 QR 이미지(`kakaopay_qr.jpg`) 모달 구축.
   - 분리된 JavaScript 모듈 스크립트들을 의존성 순서에 맞춰 로드.

2. **`style.css`**
   - 모바일 전용 미디어 쿼리를 전면 제거하고 1024x768 최소 PC 해상도 고정 및 세로 잘림 방지(`max-height: 80vh`, 독립 스크롤) 적용.
   - 728x90 광고 배너 수용을 위해 카드 및 모달 너비를 `width: min(96%, 800px)`로 확장.
   - `.ad-banner-box` 최소 높이를 106px로 확보하여 Layout Shift 방지.
   - OBS 브라우저 소스 투명 오버레이(`body.obs-overlay`) 및 크로마키 배경 스타일 추가.

3. **`js/config.js`**
   - 카카오 애드핏 728x90 배너 ID 관리 및 DOM 로드 완료 시 `initializeAds()` 실행.

4. **`js/wordPacks.js`**
   - `getNextMonsterData()`를 통한 시청자 닉네임(부족 시 `[BOT]`)과 clean 제시어 쌍 분리 제공.
   - `getHangulStrokeCount()` 한글 자모 획수 정밀 분석 유틸리티 제공.

5. **`js/audio.js`**
   - Web Audio API 기반 오타 경고음, 스테이지 업 팡파르, 레이저/폭발/피버 효과음 처리.

6. **`js/chatIntegration.js`**
   - 배열 기반 다중 채널 동시 연동 엔진 (`channels[]`) 관리.
   - 시청자 부족 시 `[BOT]` 가상 시청자 자동 채우기 연동.

7. **`js/core/StateManager.js`**
   - State Machine (MENU, SETTING, READY, PLAYING, PAUSE, GAME OVER, RESULT) 제어 및 UI 팝업 연동.
   - 무한 Stage/웨이브 진행 상태, 점수, 플레이어 체력(HP) 변수 관리.
   - `calculateRankGrade()` 등급 환산 및 `saveLeaderboardRecord()` 로컬스토리지 전적 저장.

8. **`js/core/TurretManager.js`**
   - 참여 인원(1~6명)에 따른 Canvas 하단 포탑 N개 좌표 계산 및 균등 배치.
   - **통합 입력모드**: 타깃 몬스터와 가장 가까운 포탑 자동 선정, 회전 각도($\theta$) 및 사격 궤적 연산.
   - **개별 입력모드**: P1~PN 지정 포탑별 레이저 발사 요청 처리.

9. **`js/core/MonsterManager.js`**
   - Max Monster Cap (15마리) 제한 및 스폰 대기열(Queue) 관리.
   - 5 Stage 단위 보스전(WARNING) 및 일반 낙하 몬스터 속도/좌표 업데이트.
   - 몬스터 바닥 도달 시 체력 차감 및 화면 제거 판정.

10. **`js/core/InputManager.js`**
    - 단일/개별 입력창 이벤트 수신 및 IME(한글 조합 완료) 감지.
    - `wordPacks.js`와 연동하여 한글 자모 획수 기반 정밀 CPM/WPM 실시간 계산.

11. **`js/renderers/CanvasRenderer.js`**
    - `devicePixelRatio` 스케일링 적용으로 Retina/4K 대응 Canvas 고해상도 Draw.
    - **2단 몬스터 UI 렌더링**: 상단 Pill Tag(닉네임/BOT) + 하단 Target Box(제시어) 분리 Draw.
    - 레이저 사격 이펙트, 폭발 파티클, FEVER 모드 및 Boss WARNING 연출 전담.

12. **`js/game.js`**
    - 분리된 모듈들을 중앙에서 통제하는 **오케스트레이터(Orchestrator)**.
    - 메인 루프(`requestAnimationFrame`)를 실행하며 매 프레임 업데이트 및 렌더링 명령 하달.